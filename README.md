🌦️ Season Switcher App
📌 Overview

The Season Switcher App is a simple web application built using HTML, CSS, and JavaScript. It allows users to switch between different seasons (Spring, Summer, Autumn, Winter) and dynamically updates the displayed images for small and medium screen sizes.

This project helps beginners understand DOM manipulation, functions, and dynamic image updates in JavaScript.

🧩 Features

Switch between Spring, Summer, Autumn, and Winter

Updates images dynamically based on user interaction

Supports two image sizes (small and medium)

Clean and beginner-friendly JavaScript logic

🛠️ Technologies Used

HTML5

CSS3

JavaScript (DOM Manipulation)

⚙️ How It Works

Two image elements are selected using document.getElementById().

Each season has predefined image URLs for small and medium sizes.

When a season button is clicked:

The corresponding JavaScript function is called

The src attribute of both images is updated dynamically
